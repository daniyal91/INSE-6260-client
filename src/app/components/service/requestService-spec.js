describe('requestService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(requestService) {

    //expect(requestService.doSomething()).toEqual('something');

  }));

});
