describe('availabilityService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(availabilityService) {

    //expect(availabilityService.doSomething()).toEqual('something');

  }));

});
