describe('nurseDetailService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(nurseDetailService) {

    //expect(nurseDetailService.doSomething()).toEqual('something');

  }));

});
